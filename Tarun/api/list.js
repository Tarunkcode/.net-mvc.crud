var arr = [5];
// alert("give your two inputs");
prompt(x, y);
function api(x, y) {
  return `your have ${(arr = [
    x * y * 1,
    x * y * 2,
    x * y * 3,
    x * y * 4,
    x * y * 5,
  ])}
 `;
}
